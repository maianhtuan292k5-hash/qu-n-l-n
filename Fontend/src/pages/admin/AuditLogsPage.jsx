import { useState } from 'react'
import { getAuditLogs } from '../../services/auditService'

function AuditLogsPage() { const [logs] = useState(getAuditLogs()); function exportCsv() { const header = 'Thời gian,Hành động,Đối tượng,Từ trạng thái,Đến trạng thái\n'; const body = logs.map((log) => `${log.createdAt},${log.action},${log.entity},${log.from || ''},${log.to || ''}`).join('\n'); const blob = new Blob([header + body], { type: 'text/csv;charset=utf-8' }); const url = URL.createObjectURL(blob); const link = document.createElement('a'); link.href = url; link.download = 'internconnect-audit-logs.csv'; link.click(); URL.revokeObjectURL(url) } return <section><div className="dashboard-page-heading"><div><p className="dashboard-eyebrow">QUẢN TRỊ HỆ THỐNG</p><h1>Lịch sử thao tác</h1><p>Truy vết các thay đổi quan trọng trong hệ thống.</p></div><button className="dashboard-secondary-button" type="button" onClick={exportCsv}>Xuất CSV</button></div><div className="overflow-x-auto rounded-xl border border-[#dce9f7] bg-white"><table className="w-full min-w-[700px] text-left text-xs"><thead className="bg-[#f5f8fc] text-[#7890ad]"><tr><th className="p-4">Thời gian</th><th className="p-4">Người thực hiện</th><th className="p-4">Hành động</th><th className="p-4">Đối tượng</th><th className="p-4">Thay đổi</th></tr></thead><tbody>{logs.map((log) => <tr className="border-t border-[#edf2f7]" key={log.id}><td className="p-4 text-[#7890ad]">{new Date(log.createdAt).toLocaleString('vi-VN')}</td><td className="p-4 font-semibold text-[#173b83]">{log.actor}</td><td className="p-4 text-[#526f94]">{log.action}</td><td className="p-4 text-[#526f94]">{log.entity}</td><td className="p-4 text-[#0757c9]">{log.from || '-'} → {log.to || '-'}</td></tr>)}</tbody></table>{!logs.length && <p className="p-10 text-center text-sm text-[#7890ad]">Chưa có lịch sử thao tác.</p>}</div></section> }

export default AuditLogsPage
